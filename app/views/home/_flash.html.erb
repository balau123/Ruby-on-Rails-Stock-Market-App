<% flash.each do |name, msg| %>
    <div class="alert alert-warning alert-dismissable" role="alert">
      <button class="close" data-dismiss="alert">
        <i class="glyphicon-remove-circle glyphicon"></i>
        <%= content_tag(:div, msg) %>
      </button>
    </div>
<% end %>